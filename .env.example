# App (Expo). Crear .env en la raíz con EXPO_PUBLIC_API_URL para producción
# En emulador Android usar: http://10.0.2.2:3002
# En dispositivo físico usar la IP de tu máquina (ej: http://192.168.1.100:3002)
EXPO_PUBLIC_API_URL=http://127.0.0.1:3002

# API (carpeta api/). Crear api/.env con credenciales AWS
# AWS_REGION=eu-west-1
# DDB_USUARIOS=igp_usuarios
# DDB_LOCALES=igp_Locales   (nombre exacto en AWS; DynamoDB distingue mayúsculas/minúsculas)
# DDB_EMPRESAS=igp_Empresas
# DDB_ALMACENES=igp_Almacenes
# DDB_PRODUCTOS=igp_Productos
# GOOGLE_MAPS_API_KEY=...  (opcional; autocompletado de direcciones en Locales. Si no se configura, se usa Nominatim/OpenStreetMap sin API key; el servidor envía User-Agent identificativo según política de Nominatim)
# GEOAPI_KEY=...  (opcional; rellenar municipio y provincia al introducir código postal en Empresas y Locales; obtener clave en https://geoapi.es)
# PORT=3001
# Agora (solo lectura). Para comprobar conexión: GET /api/agora/test-connection
# AGORA_API_BASE_URL=http://SERVIDOR:8984
# AGORA_API_TOKEN=...
# Productos Ágora (sync a DynamoDB para lectura rápida). Ver api/AGORA-PRODUCTS-SYNC.md
# DDB_AGORA_PRODUCTS_TABLE=Igp_AgoraProducts
# AGORA_PRODUCTS_SYNC_THROTTLE_MINUTES=30  (no llamar a Ágora si sync fue hace menos de N min; ?force=1 para forzar)
# Cierres de ventas (sync desde Ágora PosCloseOuts/SystemCloseOuts)
# DDB_SALES_CLOSEOUTS_TABLE=Igp_SalesCloseouts
# Gestión festivos y estimaciones (Comparativa Fechas Cajas)
# DDB_GESTION_FESTIVOS_TABLE=Igp_Gestionfestivosyestimaciones
# SYNC_CLOSEOUTS_ENABLED=true  (por defecto false: sync manual desde la app con el botón Sincronizar)
# Roles y permisos (menú y pantallas). Ver api/ROLES-PERMISOS.md
# DDB_ROLES_PERMISOS_TABLE=Igp_RolesPermisos